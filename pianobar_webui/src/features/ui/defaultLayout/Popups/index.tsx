import React from "react";
import Disconnected from "./Disconnected";

const Popups = () => (
    <React.Fragment>
        <Disconnected />
    </React.Fragment>
);

export default React.memo(Popups);
